"""
CTS0004 - PERSISTENT member order changed vs the git base.
Thin rule file: metadata + one check; logic lives in rules/impl.
"""

from cds_text_sync.analyze.capabilities import Capability, Scope
from cds_text_sync.analyze.rules.impl.persistent_order import check
from cds_text_sync.analyze.rules_api import RuleSpec

RULE = RuleSpec(
    id="CTS0004",
    title="PERSISTENT order changed",
    severity="danger",
    scope=Scope.HISTORY,
    requires={Capability.GIT_BASE},
    kinds="gvl_persistent",
    summary="Order of persistent variables changed against the git base.",
    check=check,
)
