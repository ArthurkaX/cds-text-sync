"""
CTS0003 - literal ExplicitColor beside a live NamedColor.
Thin rule file: metadata + one check; logic lives in rules/impl.
"""

from cds_text_sync.analyze.capabilities import Capability, Scope
from cds_text_sync.analyze.rules.impl.visu_color import check
from cds_text_sync.analyze.rules_api import RuleSpec

RULE = RuleSpec(
    id="CTS0003",
    title="Dead explicit color",
    severity="style",
    scope=Scope.UNIT,
    requires={Capability.VISU_XML},
    kinds="visualization",
    summary="ExplicitColor literal ignored because a NamedColor link wins.",
    check=check,
)
