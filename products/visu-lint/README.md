# visu-lint

Machine-facing validation for generated CODESYS visualization XML.

This product owns the JSON finding contract for `cts visu-lint`. It is
independent from both the human `.st` static analyzer and the `cts visu`
authoring/synchronization helpers. The latter remain in `cds-text-sync`.
